import React from "react";
import settingsIcon from "../../assets/icon-settings.svg";

const GearButton = () => {
  return (
    <img
      src={settingsIcon}
      alt="settings"
    />
  );
};

export default GearButton;
